const UserProfile = ({ name }) => {
  return <div>Hello, {name}! This is your profile page.</div>;
};

// Theme config
const customTheme = {
  border: "2px solid teal",
  fontFamily: "monospace",
  padding: "1rem",
  margin: "1rem",
};

export default UserProfile;
